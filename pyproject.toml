[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "factum-fic"
version = "0.1.0"
description = "Daemon CLI + folder watcher per registrazione automatica fatture su Fatture in Cloud tramite Factum Parse API"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "AGPL-3.0-or-later" }
authors = [
    { name = "Fabrizio Terzi", email = "info@pyragogy.org" },
]

classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Financial and Insurance Industry",
    "License :: OSI Approved :: GNU Affero General Public License v3 or later (AGPLv3+)",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business :: Financial :: Accounting",
]

dependencies = [
    "httpx >= 0.27.0",
    "typer[all] >= 0.12.0",
    "rich >= 13.7.0",
    "pydantic >= 2.7.0",
    "pydantic-settings >= 2.2.0",
    "pyyaml >= 6.0.1",
    "watchdog >= 4.0.0",
    "tenacity >= 8.3.0",
    "pypdf>=6.16.1",
]

[project.optional-dependencies]
dev = [
    "pytest >= 8.0.0",
    "ruff >= 0.4.0",
    "reportlab >= 4.0.0",
]

[project.scripts]
factum-fic = "factum_fic.cli.main:app"

[tool.hatch.build.targets.wheel]
packages = ["src/factum_fic"]

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "SIM"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
asyncio_mode = "auto"

[tool.coverage.run]
source = ["factum_fic"]

[dependency-groups]
dev = [
    "pytest-asyncio>=1.4.0",
    "reportlab>=4.0.0",
]